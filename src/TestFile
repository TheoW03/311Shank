define TestFunction34(a,b,c:integer)
constants
 integer Fib:=9+8
 integer FibNumAtN2 := 0(*comment*)
variables
  integer FibNumAtn := 0 (*hi*)
begin
  FibN := Fib=90
  FibN := b+2*a
  if FibN = Fib then
  begin
    if FibN = a then
    begin
        Fib2 := 0
    end
    Fib2 := 1+1
  end
end